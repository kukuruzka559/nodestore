<?php
// 1. Очищаем куку username, устанавливая время истечения на час назад
if (isset($_COOKIE['email'])) {
    // Устанавливаем пустое значение и время -3600 (прошлое)
    // Важно: укажи тот же путь '/', который использовался при создании куки
    setcookie('email', '', time() - 3600, '/');
}

// 2. Если ты использовал стандартные сессии PHP, их тоже стоит очистить
session_start();
session_unset();
session_destroy();

// 3. Редирект на главную страницу
header('Location: ../index.php');
exit;
?>